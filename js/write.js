import{auth,db}from"./firebase.js";import{onAuthStateChanged}from"https://www.gstatic.com/firebasejs/12.16.0/firebase-auth.js";import{collection,addDoc,serverTimestamp}from"https://www.gstatic.com/firebasejs/12.16.0/firebase-firestore.js";let user;onAuthStateChanged(auth,u=>{user=u;if(!u)location.href="auth.html"});async function save(status){if(!title.value.trim()||!content.value.trim())return msg.textContent="أكمل العنوان والنص";try{await addDoc(collection(db,"posts"),{userId:user.uid,author:user.displayName||"كاتب",title:title.value.trim(),type:type.value,content:content.value.trim(),tags:tags.value.split(/[،,]/).map(x=>x.trim()).filter(Boolean),status,likes:0,commentsCount:0,createdAt:serverTimestamp()});msg.textContent=status==="published"?"تم نشر النص":"تم حفظ المسودة";title.value=content.value=tags.value=""}catch(e){msg.textContent=e.code}}publishBtn.onclick=()=>save("published");draftBtn.onclick=()=>save("draft");