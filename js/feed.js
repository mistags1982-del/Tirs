const esc=(s="")=>{const d=document.createElement("div");d.textContent=String(s);return d.innerHTML};import{auth,db}from"./firebase.js";import{collection,getDocs,query,where,doc,getDoc}from"https://www.gstatic.com/firebasejs/12.16.0/firebase-firestore.js";const snap=await getDocs(query(collection(db,"posts"),where("status","==","published")));let a=snap.docs.map(d=>({id:d.id,...d.data()})).sort((a,b)=>(b.createdAt?.seconds||0)-(a.createdAt?.seconds||0));feed.innerHTML=a.length?a.map(p=>`<article class="post"><div class="post-head"><div><a href="profile.html?id=${p.userId}"><b>${esc(p.author)}</b></a></div></div><h2><a href="post.html?id=${p.id}">${esc(p.title)}</a></h2><div class="content">${esc(p.content)}</div><p>${(p.tags||[]).map(t=>`<span class="tag">#${esc(t)}</span>`).join("")}</p><div class="actions"><a href="post.html?id=${p.id}">إعجاب وتعليق</a></div></article>`).join(""):"لا توجد نصوص منشورة بعد.";